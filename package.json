{
  "name": "ems-root",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "push": "git add . && git commit -m \"automated update\" && git push origin main"
  }
}
